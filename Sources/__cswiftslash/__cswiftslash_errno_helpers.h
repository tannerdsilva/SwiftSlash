#ifndef _CSWIFTSLASH_ERRNO_HELPERS_H
#define _CSWIFTSLASH_ERRNO_HELPERS_H

#include <errno.h>

// /// @brief returns the current errno value from the global variable. this is difficult to do in Swift for whatever reason so this function is used to retireve that.
// /// @return the current errno value.
// int _cswiftslash_errno_get();

#endif // _CSWIFTSLASH_ERRNO_HELPERS_H